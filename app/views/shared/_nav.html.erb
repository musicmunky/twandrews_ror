<!-- Fixed navbar -->
<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("#{Rails.application.config.app_name}", root_path(), { class: "navbar-brand" }) %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
<!--
        <li class="dropdown <%#= is_active('some_route')%>">
          <%#= link_to(root_path(), { class: "dropdown-toggle", "data-toggle": "dropdown", "role": "button", "aria-expanded": "false" }) do %>
            [Some menu dropdown] <span class="caret"></span>
          <%# end %>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#">Projects</a></li>
            <li><a href="#">Resume</a></li>
            <li class="divider"></li>
            <li class="dropdown-header">Nav header</li>
            <li><a href="#">Separated link</a></li>
            <li><a href="#">One more separated link</a></li>
          </ul>
        </li>
 -->
        <li class="<%= is_active('projects') %>"><%= link_to("Projects", projects_path) %></li>
        <li class="<%= is_active('resume') %>"><%= link_to("Resume", resume_path) %></li>
		<li class="<%= is_active('about') %>"><%= link_to("About", about_path) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">

		<% if user_signed_in? %>
			<li>
				<%= link_to(current_user.get_name, edit_user_registration_path, { class: "navlink", style: "text-decoration:none;color:#9D9D9D" }) %> |
				<%= link_to('Logout', destroy_user_session_path, { :method => :delete, style: "text-decoration:none;color:#9D9D9D" }) %>
			</li>
		<% else %>
        <li>
			<%= link_to('Login', new_user_session_path, { class: "btn btn-primary navbar-btn", style:"text-decoration:none;color:#FFF;padding-top:5px;padding-bottom:5px;" })  %>
		  </li>
		<% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>


